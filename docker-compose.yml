version: "3"
services: 
  nginx:  
    restart: always
    image:
      'nginx:latest'
    build:
      dockerfile: Dockerfile.dev
      context: ./nginx
    ports:
      - '3050:3051'
    depends_on:
      - api
      - client